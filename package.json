{
	"name": "auth-backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "set NODE_ENV=development && nodemon --ignore \"./data\" server.js",
		"start": "set NODE_ENV=development && nodemon server.js"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^18.11.5"
	},
	"dependencies": {
		"express": "^4.18.2"
	}
}
